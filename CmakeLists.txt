cmake_minimum_required(VERSION 3.10)

# Project name and version
project(ProjectName VERSION 1.0)

# Use C++17
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED True)

# Set source and header files
set(SOURCES
    src/main.cpp
    src/utils.cpp
    src/color.cpp
    src/cursor_input.cpp
)

set(HEADERS
    src/headers/color.hpp
    src/headers/utils.hpp
    src/headers/cursor_input.hpp
)

# Exe
add_executable(ProjectName ${SOURCES} ${HEADERS})

target_include_directories(ProjectName PRIVATE src/headers)

# Optional: copy asset folder into build dir
file(COPY assets DESTINATION ${CMAKE_BINARY_DIR})
